{
  "title": "Add two integers",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "scraper_type": {
      "title": "Scraper Type",
      "type": "string",
      "description": "The type of scraper you want to use.",
      "enum": [
        "Company Follower",
        "Post Liker"
      ],
      "editor": "select",
      "prefill": "Company Follower"
    },
    "company_url": {
      "title": "Company Url",
      "type": "string",
      "description": "The company URL you want to scrape follower information from.",
      "editor": "textfield",
      "prefill": "https://www.example.com",
      "pattern": "^https://www\\.linkedin\\.com/company/\\d+/admin/dashboard/$"
    },
    "follower_number": {
      "title": "Follower number",
      "type": "integer",
      "description": "The follower count you want to scrape.",
      "editor": "number",
      "prefill": 2
    },
    "post_url": {
      "title": "Post Url",
      "type": "string",
      "description": "The post URL you want to scrape liker information from.",
      "editor": "textfield",
      "prefill": "https://www.example.com",
      "pattern": "^https://www\\.linkedin\\.com/feed/update/urn:li:activity:[a-zA-Z0-9]+$"
    }
  },
  "required": [
    "scraper_type"
  ]
}