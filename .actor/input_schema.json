{
  "title": "Add two integers",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "scraper_type": {
      "title": "Scraper Type",
      "type": "string",
      "description": "The type of scraper you want to use.",
      "enum": [
        "Company Follower",
        "Post Liker"
      ],
      "editor": "select",
      "prefill": "Company Follower"
    }
  },
  "oneOf": [
    {
      "properties": {
        "scraper_type": {
          "enum": [
            "Company Follower"
          ]
        },
        "company_url": {
          "title": "Company Url",
          "type": "string",
          "description": "The company URL you want to scrape follower information from.",
          "editor": "textfield",
          "prefill": "https://www.example.com"
        },
        "follower_number": {
          "title": "Follower number",
          "type": "integer",
          "description": "The follower count you want to scrape.",
          "editor": "number",
          "prefill": 2
        }
      },
      "required": [
        "company_url",
        "follower_number"
      ]
    },
    {
      "properties": {
        "scraper_type": {
          "enum": [
            "Post Liker"
          ]
        },
        "post_url": {
          "title": "Post Url",
          "type": "string",
          "description": "The post URL you want to scrape liker information from.",
          "editor": "textfield",
          "prefill": "https://www.example.com"
        }
      },
      "required": [
        "post_url"
      ]
    }
  ],
  "required": [
    "scraper_type"
  ]
}